# PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/tapin"

# Session auth (jose)
# Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-string"

# Next.js
NEXT_TELEMETRY_DISABLED=1
